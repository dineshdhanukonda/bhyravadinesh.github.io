<!DOCTYPE html>

<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>TradingView → n8n → Telegram Intraday Alerts Guide</title>
  <style>
    :root{
      --brand:#0d6efd;
      --bg:#f4f6f9;
      --card:#ffffff;
      --ink:#333;
      --muted:#6b7280;
      --warning:#fff3cd;
    }
    *{box-sizing:border-box}
    body{font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; margin:0; background:var(--bg); color:var(--ink)}
    header{background:var(--brand); color:#fff; padding:1.75rem 1rem; text-align:center; box-shadow:0 4px 6px rgba(0,0,0,.1)}
    header h1{margin:.2rem 0;font-size:1.6rem}
    header p{margin:0;opacity:.95}

```
nav{background:#fff; padding:.75rem 1rem; box-shadow:0 2px 4px rgba(0,0,0,.08); position:sticky; top:0; z-index:10}
nav ul{display:flex; flex-wrap:wrap; justify-content:center; gap:1rem; margin:0; padding:0; list-style:none}
nav a{text-decoration:none; color:var(--brand); font-weight:600}
nav a:hover{text-decoration:underline}

.container{max-width:1000px; margin:0 auto; padding:1rem}
section.card{background:var(--card); margin:1rem auto; padding:1.5rem; border-radius:16px; box-shadow:0 4px 12px rgba(0,0,0,.08)}
section.card h2{color:var(--brand); margin-top:0}
.note{background:var(--warning); border-left:6px solid #ffecb5; padding:.75rem; border-radius:8px; margin:1rem 0}
.grid{display:grid; gap:1rem}
@media(min-width:768px){.grid.cols-2{grid-template-columns:1fr 1fr}}

pre{background:#0b1020; color:#e6edf3; padding:1rem; border-radius:12px; overflow:auto; line-height:1.4}
code{font-family:Consolas, Menlo, Monaco, monospace; font-size:.92rem}

.btns{display:flex; gap:.5rem; flex-wrap:wrap; margin:.5rem 0 0}
.btn{background:#fff; color:#111; border:1px solid #e5e7eb; padding:.45rem .75rem; border-radius:12px; cursor:pointer; box-shadow:0 1px 2px rgba(0,0,0,.06)}
.btn:hover{box-shadow:0 2px 6px rgba(0,0,0,.1)}
.small{font-size:.85rem; color:var(--muted)}

footer{ text-align:center; padding:2rem 1rem; color:#fff; background:var(--brand); margin-top:2rem }
```

  </style>
</head>
<body>
  <header>
    <h1>TradingView → n8n → Telegram Intraday CE/PE Alerts</h1>
    <p>Single-file guide with full Pine Script + n8n JSON + setup steps</p>
  </header>

  <nav>
    <ul>
      <li><a href="#flow">Flow</a></li>
      <li><a href="#pine">Pine Script</a></li>
      <li><a href="#alert">Alerts</a></li>
      <li><a href="#n8n">n8n Workflow</a></li>
      <
